## 1. 软件包与测试基础

- [x] 1.1 将占位项目转换为 `src/uim` 软件包，暴露 `uim` console script，将 `requires-python` 设为 `>=3.9`，并验证 `uv run uim --help` 与 `uv run uim --version` 成功。
- [x] 1.2 增加兼容 Python 3.9 的 Click、平台路径处理、保留注释 TOML 和 pytest 运行及测试依赖，并验证 `uv lock` 与 `uv sync` 成功。
- [x] 1.3 为隔离的 home、XDG、Windows 风格目录、项目、环境变量和畸形配置建立单元测试夹具，并验证夹具冒烟测试不会访问真实用户配置。

## 2. 索引目录管理

- [x] 2.1 实现 PyPI、清华、阿里云、腾讯、华为和中科大的不可变内置目录，并验证测试覆盖每个名称、HTTPS Simple URL 和主页。
- [x] 2.2 实现符合平台规范的自定义目录加载与原子持久化，并验证往返测试保留有效的自定义名称、URL 和可选主页。
- [x] 2.3 实现名称与 URL 校验、内置名称遮蔽保护、凭据拒绝和错误脱敏，并验证反向测试保持目录文件不变且不泄露 URL 用户信息。
- [x] 2.4 实现目录条目的添加、删除和重命名，包括使用中条目保护，并验证命令测试覆盖成功、重复、缺失、内置项修改及使用中自定义项删除。

## 3. uv 配置解析

- [x] 3.1 实现 Linux、macOS 和 Windows 约定下的用户、系统与本地 uv 配置路径发现，并在模拟环境中用表驱动测试验证。
- [x] 3.2 为已有 `uv.toml`、已有 `pyproject.toml` 和未配置目录实现本地目标选择，并用临时目录树验证最近文件与同目录覆盖规则。
- [x] 3.3 实现带来源的生效默认索引解析，覆盖环境、项目、用户、系统、旧版标量、结构化默认项及隐式 PyPI，并验证优先级符合 uv 文档。
- [x] 3.4 实现仅用于目录匹配的 URL 规范化，不改变保存或显示的 URL，并验证末尾斜杠等价和未知 URL 行为。

## 4. 事务式索引切换

- [x] 4.1 实现保留注释的 `uv.toml` 顶层 `default-index` 与 `pyproject.toml` 中 `[tool.uv]` 修改，包括旧版标量规范化，并验证夹具保留无关设置、补充索引和注释。
- [x] 4.2 实现对不受管理的结构化默认索引冲突的检测和安全拒绝，并验证冲突夹具逐字节不变且返回可执行错误。
- [x] 4.3 实现同目录临时写入、验证、权限保留、刷新和原子替换，并验证注入的解析、写入和替换失败会保持原配置可用。
- [x] 4.4 实现全局 `uim use [name]` 切换和交互式选择，并验证成功、未知名称、交互式、非交互式以及更高优先级覆盖警告。
- [x] 4.5 实现 `uim use [name] --local`，创建新 `uv.toml` 时要求交互确认或自动化显式确认，并验证它绝不修改用户配置且命中预期项目文件。
- [x] 4.6 支持原地更新用户级 `uv.toml` 中只含 `name`、`url`、`default` 的简单命名默认索引，并验证复杂语义、项目级命名默认项和重复目标名称仍安全拒绝且文件不变。

## 5. 与 nrm 兼容的只读命令

- [x] 5.1 实现 `uim current`、`--show-url`/`-u` 和详细来源输出，并验证目录匹配、未知 URL、项目覆盖与环境覆盖。
- [x] 5.2 实现与 nrm 熟悉格式一致的 `uim ls` 对齐和唯一生效标记，并验证已知、未知及隐式 PyPI 的输出快照。
- [x] 5.3 通过无 shell 的平台浏览器启动实现 `uim home <name> [browser]`，并验证默认浏览器、指定浏览器、缺失主页和启动失败。
- [x] 5.4 为单个或全部条目实现有界并发 HTTPS 可达性测试及确定输出顺序，并验证成功、超时、TLS、重定向、HTTP 失败、延迟和不修改配置。
- [x] 5.5 使用带 `uim/<version>` 标识的 `HEAD` 请求探测轻量 `pip/` 项目端点，只有 HTTP 405/501 时才回退到最多读取一个字节的 `Range: bytes=0-0` GET；验证测试不会 GET 根索引，同时保留超时、TLS、重定向、脱敏、延迟、并发及不修改配置行为。

## 6. 集成、兼容性与文档

- [x] 6.1 增加端到端 CLI 测试，覆盖用户级和本地作用域下完整的 add/use/current/ls/rename/use/del 生命周期，并验证隔离测试套件不会触及开发者真实 uv 文件。
- [x] 6.2 增加从 Python 3.9 开始的受支持 Python CI 矩阵，并覆盖 Linux、macOS 和 Windows，验证工作流在每个平台运行单元测试与 CLI 测试。
- [x] 6.3 用安装说明、nrm 到 uim 迁移示例、命令参考、配置优先级、冲突恢复、安全边界和 `--local` 行为替换占位 README，并验证每个文档命令都有帮助或自动化冒烟测试覆盖。
- [x] 6.4 运行格式化、静态检查、完整测试套件、软件包构建及 wheel 安装冒烟测试，并验证干净环境中存在可用的 `uim` 命令，且没有未测试的规划需求。
